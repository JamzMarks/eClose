export const DISCOVER_PAGE_SIZE = 15;

/**
 * Quando `true`, `EventService` e `MarketplaceService` devolvem dados locais (sem HTTP).
 * A UI chama sempre as mesmas interfaces.
 */
export const USE_MOCK_DISCOVER = true;
